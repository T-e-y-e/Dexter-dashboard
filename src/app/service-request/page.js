import { Topbar } from "@/components/topbar";
import React from "react";

const page = () => {
  return (
    <div>
      <Topbar pageTitle={"Service Request"} />
    </div>
  );
};

export default page;
